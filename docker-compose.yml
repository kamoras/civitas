# For development or simple usage: docker compose up -d
# For production deploys: docker stack deploy -c docker-compose.yml
#   -c docker-compose.swarm.yml civitas (see docker-compose.swarm.yml for
#   what that overlay changes and why — the short version is: no more
#   deploy.sh, Swarm's own rolling-update + healthcheck-gated rollback
#   replaces the hand-rolled blue/green port-flip script).
name: civitas
services:
  # 2026-08: ollama was bundled here as the LLM_BACKEND=ollama option's
  # runtime, but the default (and the only backend this project actually
  # runs in production) is LLM_BACKEND=llama-server — the service defined
  # below, not this one. Confirmed via a live production audit that the
  # bundled ollama
  # container had sat completely idle since the switch: zero application
  # traffic, only its own baked-in Docker healthcheck (`ollama list`
  # every 30s) — while reserving 6G of RAM on a 15.8G Pi for a service
  # nothing was calling. Removed from the default stack; the
  # LLM_BACKEND=ollama code path itself is untouched (see
  # ollama_client.py, .env.example) for anyone who wants to run their own
  # ollama elsewhere and point OLLAMA_BASE_URL at it — add the service
  # block back (this file's git history has the exact definition) if you
  # want it bundled again.

  # 2026-08-29: live-benchmarked the from-source ARM-optimized build
  # (-mcpu=cortex-a76+dotprod+fp16) against this stock image on the same
  # Pi with the same model: prompt-processing was ~12% faster on the
  # custom build, but token-generation speed -- what actually dominates
  # wall-clock for every real call here (summaries, full stories up to
  # 2048 tokens) -- was statistically identical. Not worth hand-rolling a
  # build pipeline for ~2s saved on a once-daily batch job when the
  # official image lets Dependabot bump it like every other dependency.
  llama-server:
    image: ghcr.io/ggml-org/llama.cpp:server@sha256:bb89e76130acc65e7c92ccc8fd3100c544e2b370ccd46ff6304caf47233c7188
    container_name: mp-llama-server
    ports:
      - "127.0.0.1:8070:8070"
    volumes:
      # Bind mount, not a named volume -- the model is a single static
      # file the operator provides (see README's LLM Backend Options),
      # not app-managed state.
      - ${LLAMA_MODELS_DIR:-./llama-models}:/models:ro
    command:
      - -m
      - /models/${LLAMA_MODEL_FILE:-lfm2.5-1.2b-instruct-q4_k_m.gguf}
      - --host
      - "0.0.0.0"
      - --port
      - "8070"
      - --threads
      - "4"
      - --ctx-size
      - "4096"
      - --flash-attn
      - "on"
      - --cont-batching
      # 0 (default 8): disables context-checkpoint save/restore. Live
      # journalctl investigation (2026-08-29) found this exact path
      # crashing the server ("restored context checkpoint" -> "decode:
      # failed to initialize batch" -> HTTP 500) on near-duplicate prompts
      # (e.g. an Action Center grounding retry). Upstream's own fix
      # (ggml-org/llama.cpp#20819, #26004) is still an open, unmerged PR.
      # Trade-off: near-duplicate prompts reprocess in full instead of
      # resuming from a cached checkpoint -- slower, not incorrect.
      - --ctx-checkpoints
      - "0"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 2G
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8070/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: civitas-backend:${IMAGE_TAG:-latest}
    container_name: mp-backend
    ports:
      - "127.0.0.1:8000:8000"
    env_file: .env
    extra_hosts:
      # Only still needed for LLM_BACKEND=ollama pointed at a host-level
      # ollama install (OLLAMA_BASE_URL) -- llama-server is in-stack now
      # and reached by service DNS instead.
      - "host.docker.internal:host-gateway"
    environment:
      - DATABASE_URL=sqlite:////data/civitas.db
      - LLM_BACKEND=llama-server
      - LLAMA_SERVER_URL=http://llama-server:8070
    volumes:
      - app_data:/data
    restart: unless-stopped
    # See ollama's logging block above for why every service has one.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 4G
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: civitas-frontend:${IMAGE_TAG:-latest}
    container_name: mp-frontend
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=/api
    # No depends_on — Swarm has no concept of startup-order dependencies
    # between services (each task's lifecycle is independent) and rejects
    # the long `condition: service_healthy` form outright ("must be a
    # list"); `docker compose config`'s resolver re-expands *any* source
    # syntax (including a plain short-form list) back into that same
    # rejected long form, so there's no source syntax that survives the
    # resolve-then-stack-deploy path. Frontend not strictly waiting for
    # backend on a fresh `docker compose up` is a minor, harmless local-
    # dev-only difference — it'll just show a fetch error in the UI for
    # the few seconds until backend's healthcheck passes.
    restart: unless-stopped
    # See ollama's logging block above for why every service has one.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      # 127.0.0.1, not localhost — the container's /etc/hosts resolves
      # "localhost" to ::1 (IPv6) first, and Next's standalone server only
      # binds the IPv4 wildcard (0.0.0.0), not dual-stack (live-verified:
      # `wget http://127.0.0.1:3000/` returns the page fine, `wget
      # http://localhost:3000/` gets "Connection refused" on the exact
      # same running container — busybox wget doesn't fall back to a
      # second resolved address the way curl does).
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000/"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  # No `driver: local` on either volume below — the Swarm overlay
  # (docker-compose.swarm.yml) sets `external: true` on both, and Compose
  # rejects `driver` and `external` both being set for the same volume
  # even when they come from different merged files. Omitting `driver` is
  # a no-op for plain `docker compose up` dev usage (local is already the
  # default), so this stays compatible with both.
  app_data: {}
