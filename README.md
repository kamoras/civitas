# PR screenshot assets

Images referenced from pull request descriptions. This is an orphan branch:
it shares no history with `main` and is never merged, so these binaries stay
out of the repository's working tree and release history.

Generated with Playwright against production builds of both `main` and the PR
branch, with API responses mocked so the two are compared on identical data.
